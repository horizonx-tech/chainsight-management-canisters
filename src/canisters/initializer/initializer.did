type Time = int;
type InitializerCanister = service {
    deploy_vault_of : (principal) -> (principal);
    get_proxy : () -> (principal);
    get_registry : () -> (principal);
    set_registry : (principal) -> ();
};
service : () -> InitializerCanister;
